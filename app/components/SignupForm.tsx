'use client'

import React, {useState} from 'react'

const [formType, setFormType] = useState("signin")

export const SignupForm = () => {
  formType === "signin"

  return (
    <form action="">
        
    </form>
  )
}